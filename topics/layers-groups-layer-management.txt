Question: What is a layer, and how does the layer stack control what appears in front?
Answer: English: Layer holds content separately so it can be edited, moved, hidden, or combined with other content. Layer stack uses top-to-bottom order to control which visible content appears in front.
Practise it through the chapter task: build a five-layer design, name every layer, group related items, and change their order and visibility.
Bangla: Layer content আলাদা রাখে, যাতে edit, move, hide বা অন্য content-এর সঙ্গে combine করা যায়। Layer stack top-to-bottom order দিয়ে কোন visible content সামনে থাকবে তা নিয়ন্ত্রণ করে।
chapter task-এর মাধ্যমে এটি practise করুন: পাঁচ layer-এর design বানান, প্রতিটি layer-এর name দিন, related item group করুন এবং order ও visibility বদলান।

---

Question: What is the difference between a Background layer and a normal pixel layer?
Answer: English: Background layer is a special locked starting layer in some documents and normally has no transparency. Pixel layer contains raster pixels that can be painted or edited directly.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Background layer কিছু document-এর special locked starting layer এবং সাধারণত transparency থাকে না। Pixel layer raster pixel রাখে, যা সরাসরি paint বা edit করা যায়।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: How are type layers and shape layers kept editable?
Answer: English: Type layer keeps text editable until it is rasterized or otherwise converted. Shape layer holds a vector path with editable fill and stroke properties.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Type layer rasterize বা অন্যভাবে convert না করা পর্যন্ত text editable রাখে। Shape layer editable fill ও stroke property-সহ vector path ধরে রাখে।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: What is the difference between an adjustment layer and a fill layer?
Answer: English: Adjustment layer changes tone or color without directly rewriting the source layer pixels. Fill layer provides an editable solid color, gradient, or pattern with its own mask.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Adjustment layer source layer-এর pixel সরাসরি না বদলে tone বা color পরিবর্তন করে। Fill layer নিজস্ব mask-সহ editable solid color, gradient বা pattern দেয়।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: Why should I use a Smart Object layer before repeated transforms or filters?
Answer: English: Smart Object layer protects placed source content and supports reversible transforms and Smart Filters.
Use this as a workflow checkpoint before committing the edit. Select the correct layer before editing, and avoid merging or flattening the only master file.
Bangla: Smart Object layer placed source content রক্ষা করে এবং reversible transform ও Smart Filter সমর্থন করে।
edit commit করার আগে এটিকে workflow checkpoint হিসেবে ব্যবহার করুন। edit করার আগে সঠিক layer select করুন এবং একমাত্র master file merge বা flatten করবেন না।

---

Question: How do layer groups keep a large design organized?
Answer: English: Layer group keeps related layers together for organization and shared control.
Start with the default settings, change one control at a time, and inspect the result at both a useful zoom and the final viewing size.
Bangla: Layer group গুছিয়ে রাখা ও একসঙ্গে control করার জন্য related layer একত্রে রাখে।
default setting দিয়ে শুরু করুন, একবারে একটি control বদলান এবং useful zoom ও final viewing size—দুই জায়গায় result দেখুন।

---

Question: What is the difference between hiding and locking a layer?
Answer: English: Layer visibility shows or hides a layer without deleting it. Layer lock prevents selected kinds of changes such as movement, painting, or transparency edits.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Layer visibility delete না করে layer দেখায় বা লুকায়। Layer lock move, paint বা transparency edit-এর মতো নির্বাচিত change আটকায়।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: What is the difference between layer Opacity and Fill opacity?
Answer: English: Opacity controls the overall transparency of a layer, including many applied effects. Fill opacity controls the layer's main content while some layer effects can remain more visible.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Opacity অনেক applied effect-সহ layer-এর overall transparency নিয়ন্ত্রণ করে। Fill opacity layer-এর main content নিয়ন্ত্রণ করে, আর কিছু layer effect বেশি visible থাকতে পারে।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: Why should I rename and color-label layers?
Answer: English: Rename layer replaces an unclear default name with a useful content-based name. Layer color label marks layers or groups with colors to make a large file easier to scan.
Use this as a workflow checkpoint before committing the edit. Select the correct layer before editing, and avoid merging or flattening the only master file.
Bangla: Rename layer অস্পষ্ট default name-এর বদলে content অনুযায়ী useful name দেয়। Layer color label বড় file সহজে বুঝতে layer বা group-কে color দিয়ে চিহ্নিত করে।
edit commit করার আগে এটিকে workflow checkpoint হিসেবে ব্যবহার করুন। edit করার আগে সঠিক layer select করুন এবং একমাত্র master file merge বা flatten করবেন না।

---

Question: When should I duplicate or delete a layer?
Answer: English: Duplicate layer creates another editable copy of a layer for variation or safety. Delete layer removes the selected layer from the document.
Choose it only when the image and desired result match the use described above; work on a copy, mask, or separate layer when possible.
Bangla: Duplicate layer variation বা safety-এর জন্য layer-এর আরেকটি editable copy তৈরি করে। Delete layer selected layer document থেকে সরিয়ে দেয়।
image ও desired result উপরের use-এর সঙ্গে match করলেই এটি choose করুন; সম্ভব হলে copy, mask বা separate layer-এ কাজ করুন।

---

Question: What is the difference between merging layers and flattening an image?
Answer: English: Merge layers combines selected layers into fewer rasterized or consolidated layers and reduces editability. Flatten Image combines the document into a single background-style result and removes normal layer editing.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Merge layers selected layer একত্র করে কম rasterized বা consolidated layer বানায় এবং editability কমায়। Flatten Image document-কে একটিমাত্র background-style result-এ combine করে এবং normal layer editing সরায়।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: How do Align and Distribute arrange several layers?
Answer: English: Align layers lines selected layer bounds up to a chosen edge or center. Distribute layers spaces three or more selected layers consistently.
Start with the default settings, change one control at a time, and inspect the result at both a useful zoom and the final viewing size.
Bangla: Align layers selected layer-এর boundary-কে নির্বাচিত edge বা center অনুযায়ী একই লাইনে আনে। Distribute layers তিন বা তার বেশি selected layer সমানভাবে space করে।
default setting দিয়ে শুরু করুন, একবারে একটি control বদলান এবং useful zoom ও final viewing size—দুই জায়গায় result দেখুন।

---

Question: What are linked layers, and how are they different from a group?
Answer: English: Linked layers keep separate layers connected for moving or transforming together.
Test the options on duplicate layers with the same image, then compare control, edge quality, editability, and the final visible result.
Bangla: Linked layers আলাদা layer-কে যুক্ত রাখে, যাতে একসঙ্গে move বা transform করা যায়।
একই image-এর duplicate layer-এ optionগুলো test করুন; তারপর control, edge quality, editability ও final visible result compare করুন।

---

Question: What is a clipping mask, and how does the lower layer control it?
Answer: English: Clipping mask limits an upper layer's visibility to the opaque content of the layer below.
Locate the control in Photoshop, try it on a small safe file, and confirm exactly which layer, pixels, path, or output setting it changes.
Bangla: Clipping mask উপরের layer-এর visibility নিচের layer-এর opaque content-এর মধ্যে সীমিত রাখে।
Photoshop-এ controlটি খুঁজুন, small safe file-এ try করুন এবং এটি কোন layer, pixel, path বা output setting change করে তা confirm করুন।

---

Question: What is a layer style, and which common effects can it add?
Answer: English: Layer style adds editable effects such as shadow, stroke, glow, or overlay to a layer.
Locate the control in Photoshop, try it on a small safe file, and confirm exactly which layer, pixels, path, or output setting it changes.
Bangla: Layer style layer-এ shadow, stroke, glow বা overlay-এর মতো editable effect যোগ করে।
Photoshop-এ controlটি খুঁজুন, small safe file-এ try করুন এবং এটি কোন layer, pixel, path বা output setting change করে তা confirm করুন।
